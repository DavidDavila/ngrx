import { PrepareResponsePipe } from './prepare-response.pipe';

describe('PrepareResponsePipe', () => {
  it('create an instance', () => {
    const pipe = new PrepareResponsePipe();
    expect(pipe).toBeTruthy();
  });
});
